# Reservation App Frontend

This is a Vite + React + TypeScript frontend for the Reservation App.

Run locally:

```bash
npm install
npm run dev
```

Dev server proxies /api -> http://localhost:3000 (see `vite.config.ts`).
